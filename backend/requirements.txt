# ================================================================
# backend/requirements.txt — FastAPI + Celery 백엔드
# 권장 Python: 3.11.x (model/ 의 torch 호환과 동일 환경 권장)
# ================================================================
#
# 웹(FastAPI)과 워커(Celery)를 같은 가상환경에서 돌리는 것을 권장한다.
# 워커는 추가로 torch/opencv 등 ML 의존성이 필요하다(아래 + 루트 requirements.txt).
#
#   pip install -r backend/requirements.txt
#   pip install -r requirements.txt          # torch/opencv 등 ML 의존성
# ================================================================

fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=42.0        # MySQL 8 caching_sha2_password 인증용
python-multipart>=0.0.9   # 파일 업로드(Form/UploadFile)
celery>=5.3
redis>=5.0

# 영상 메타데이터 추출(업로드 시) + 추론/클립 생성(워커)
opencv-python>=4.9.0,<5.0
