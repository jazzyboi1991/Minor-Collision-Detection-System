{
  "name": "aiui-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "npm --prefix server run dev",
    "dev:ui": "npm --prefix UI run dev",
    "build": "npm --prefix UI run build",
    "check": "npm --prefix server run check && npm --prefix UI run lint"
  }
}
